name: CI

on:
  push:
  pull_request:

jobs:
  server-build:
    name: Server Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: server/package-lock.json

      - name: Install contract generator
        working-directory: .
        run: npm ci

      - name: Install server dependencies
        run: npm ci

      - name: Determine build version
        working-directory: .
        run: |
          VERSION=$(node -p "require('./server/package.json').version")
          echo "BUILD_VERSION=${VERSION}+build.${GITHUB_RUN_NUMBER}" >> "$GITHUB_ENV"
          echo "Building server version ${VERSION}+build.${GITHUB_RUN_NUMBER}"

      - name: Build server
        run: npm run build

  client-build:
    name: Client Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Install contract generator
        working-directory: .
        run: npm ci

      - name: Install client dependencies
        run: npm ci

      - name: Determine build version
        working-directory: .
        run: |
          VERSION=$(node -p "require('./client/package.json').version")
          echo "BUILD_VERSION=${VERSION}+build.${GITHUB_RUN_NUMBER}" >> "$GITHUB_ENV"
          echo "Building client version ${VERSION}+build.${GITHUB_RUN_NUMBER}"

      - name: Build client
        run: npm run build
